const kecepatan = 2
const jarak = 100

const waktu = jarak / kecepatan 

console.log(`Waktu yang diperlukan untuk menempuh jarak ${jarak} meter dengan kecepatan ${kecepatan} m/det adalah ${waktu} detik.`);
